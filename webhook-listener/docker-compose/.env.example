# Docker Compose Environment Variables Example
# Copy this file to .env and customize the values

# ============================================================================
# MLflow Webhook Secret
# ============================================================================
# This secret is used for:
# 1. MLflow to encrypt/decrypt webhook secrets in the database
# 2. Webhook listener to verify incoming webhook signatures
#
# IMPORTANT: Use the same secret for both services!
# Generate a new secret with: python -c "import secrets; print(secrets.token_urlsafe(32))"
MLFLOW_WEBHOOK_SECRET=Sl7KScz-C0S93maHQMegj4qCwNtPz7_hDqnuWm4zKGU=

# ============================================================================
# Model Creation Service Settings
# ============================================================================
# How often to cycle the "deploy" tag (in seconds)
DEPLOYMENT_STATUS_CYCLE_TIME=10

# ============================================================================
# mkcert Certificate Path (for HTTPS support)
# ============================================================================
# Path to mkcert root CA certificate
# Default: ~/.local/share/mkcert
# macOS: ~/Library/Application Support/mkcert
# MKCERT_CAROOT=~/.local/share/mkcert

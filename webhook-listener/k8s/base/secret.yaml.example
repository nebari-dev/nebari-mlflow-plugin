---
# Example Secret for MLflow Webhook Authentication
#
# Instructions:
# 1. Copy this file to secret.yaml (git-ignored)
# 2. Generate a secure random secret:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
# 3. Base64 encode the secret:
#    echo -n "your-secret-here" | base64
# 4. Replace YOUR_BASE64_ENCODED_SECRET_HERE with the encoded value
# 5. Apply to your cluster:
#    kubectl apply -f k8s/secret.yaml
# 6. Configure the same secret in MLflow webhook settings
#
# IMPORTANT: Never commit the actual secret.yaml file to version control!

apiVersion: v1
kind: Secret
metadata:
  name: mlflow-webhook-secret
  namespace: mlflow
  labels:
    app: mlflow-kserve-webhook-listener
type: Opaque
data:
  # Replace this with your base64-encoded webhook secret
  # Example: echo -n "my-secret-webhook-key" | base64
  secret: YOUR_BASE64_ENCODED_SECRET_HERE
